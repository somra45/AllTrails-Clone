import './TrailMapShow.css'

const TrailMapShow = ( {trail} ) => {
    return (
        <>
            <div className="trail-map-container">
                <h1 className=''>{trail.name}</h1>
            </div>
        </>
    )
}

export default TrailMapShow